import {ToArray, Pick, Filter, Group, Sort, Sorts, Concatenate} 
  from './utils/array/arrays.utils'; 
import {Predicate, Sorter} 
  from './utils/array/arrays.utils'; 

export type {Predicate, Sorter} 
export {ToArray, Pick, Filter, Group, Sort, Sorts, Concatenate} 