import {ToArray, Filter, Intersect, Pick, Groups, Group, Sort, Union} 
  from './utils/arrays.utils'; 
import {Comparator, Predicate, Sorter} 
  from './utils/arrays.utils'; 

export type {Comparator, Predicate, Sorter} 
export {ToArray, Filter, Intersect, Pick, Groups, Group, Sort, Union} 