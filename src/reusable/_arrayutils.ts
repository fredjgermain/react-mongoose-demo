import {ToArray, Filter, Accumulator, Indexes, Intersect, Pick, Group, Sort, Union, Duplicates} 
  from './utils/arrays.utils'; 
import {Comparator, Predicate} 
  from './utils/arrays.utils'; 

export type {Comparator, Predicate} 
export {ToArray, Filter, Accumulator, Indexes, Intersect, Pick, Group, Sort, Union, Duplicates} 