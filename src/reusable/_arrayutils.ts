import {ToArray, Filter, Indexes, Pick, Sort, Union, Duplicates} from './utils/arrays.utils'; 
import {Comparator, Predicate} from './utils/arrays.utils'; 

export type {Comparator, Predicate} 
export {ToArray, Filter, Indexes, Pick, Sort, Union, Duplicates} 